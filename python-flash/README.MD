
```bash
# Create virtual environment
python3 -m venv ~/.virtualenvs/xke-serverless
source ~/.virtualenvs/xke-serverless/bin/activate

pip install -r setup_requirements.txt
pip install -r requirements.txt

./start

```